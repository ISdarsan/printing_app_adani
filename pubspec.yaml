name: canteen_adani
description: "Canteen Billing App for Adani CSR"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # More flexible SDK range

dependencies:
  flutter:
    sdk: flutter

  # Your existing UI packages
  cupertino_icons: ^1.0.8
  lottie: ^2.7.0
  animated_text_kit: ^4.2.2

  # --- ADDED FIREBASE PACKAGES ---
  firebase_core: ^2.32.0         # The main package to connect
  cloud_firestore: ^4.17.5       # The database
  firebase_auth: ^4.19.6         # The login/authentication
  # ---------------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Updated to a newer version

flutter:
  uses-material-design: true
  assets:
    - assets/LOGO.png
    - assets/neon_ring.json
